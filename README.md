# Raytracing

## Building

### Requirements
* CMake version >= 4.00

On Linux, use the system package manager to install:
* SDL3
* glew

### How to
Use your favourite IDE with CMake support, otherwise
use the usual CMake building procedure.

On Windows, all dependencies are fetched and built by CMake,
on Linux, use the package manager
